# 세션 핸드오프 — 2026-06-14 22:10

> 배달앱(쿠팡이츠·배민) 주문을 **이지체크 TS-194N(KICC 안드로이드 POS) 내장 프린터**로 자동 출력.
> 작업 브랜치: **`android-printer`** / 레포 로컬: `/Users/jiwonpark/Claude/Projects/android-printer`

---

## 1. 이번 세션 요약 (한 일)

1. **로컬 빌드 환경 구성** — 레포 클론, JDK21+Android SDK, 빌드오류 2개 수정.
   → 커밋·푸시 완료 (`8d6c755` JVM타깃/sumOf 수정).
2. **웹 사이드로딩으로 APK 설치** — USB/네트워크가 다 막혀서, 맥에 임시 HTTP서버 + **cloudflared 터널** + **spoo.me 단축링크**로 포스기 브라우저에서 받아 설치(Play Protect "무시하고 설치").
3. **KICC 내장 프린터 출력 구현 (✅ 동작 확인)** — 제조사 앱 `POS 장치설정`(kr.co.kicc.kpos) **디컴파일**해서 내장 프린터 프로토콜 파악 → 구현.
   → 커밋·푸시 완료 (`e231733`).
4. **쿠팡 클라우드 주문 출력 (✅ 완성·동작 확인)** — Fiddler로 쿠팡 POS API 캡처 → 서버가 자동로그인+주문조회 → 앱이 받아 내장 프린터로 **실제 쿠팡이츠 영수증 레이아웃** 그대로 출력. 사용자가 "아주 잘나와" 확인.
   → ⚠️ **아직 미커밋** (아래 3번 참고).
5. **배민 (진행 중)** — API가 인증서고정(cert pinning)+PubNub이라 쿠팡식 불가. **B안(배민 PDF→변환→출력)** 으로 전환. PDF 텍스트추출→42칸 재배치 **변환 프로토타입 검증 완료**(server-bridge/baemin_reflow.py). 아직 서버/앱 통합·자동화 안 됨.

---

## 2. 현재 상태

### 동작/배포
- **내장 프린터 + 쿠팡 영수증 출력 = 정상 동작.** 최신 APK가 터널에 배포돼 있음.
- 서버·터널 **실행 중**(맥). 단 **`/tmp` 기반이라 매우 불안정** — 세션 중 `/tmp`가 한 번 비워져 서버/터널/창건물이 다 날아갔었음. 재발 가능.
- **현재 터널 URL**: `https://void-pages-sega-advertisers.trycloudflare.com`
  - 설치: `spoo.me/posapp2` / 업로드: `spoo.me/posup2`
  - ⚠️ 이 URL이 **앱 코드에 하드코딩**돼 있음(`CloudOrderClient.BASE`). 터널 재시작하면 URL이 바뀌어 앱이 깨짐 → **재빌드 필요**. (오라클 클라우드 이전이 근본 해결)

### 미커밋 변경 (⚠️ 중요 — 동작하지만 git에 안 올라감)
working tree에 살아있음(디스크라 안전하지만 커밋 안 됨):
- 신규: `core/.../printer/CoupangReceiptFormatter.kt`, `app/.../data/CloudOrderClient.kt`
- 수정: `core/.../model/Order.kt`(options/subtotal/deliveryFee/paymentMethod 필드), `app/.../printer/PrintController.kt`(플랫폼별 포맷터 분기), `app/.../ui/MainActivity.kt`(클라우드 주문 화면), `AndroidManifest.xml`(INTERNET 권한)
- ※ `SettingsStore.kt`/`KiccUdsClient.kt`/`KiccInnerPrinter.kt`/`EscPos.kt` 등은 `e231733`로 이미 커밋됨.

### /tmp 외부 보존물 (레포 안 `server-bridge/`)
- `server.py` — 쿠팡 자동로그인 + /orders + 다운로드/업로드/uplog 엔드포인트 (레포에 없던 작업물, 보존함)
- `coupang_creds.json` — 쿠팡 로그인(아이디 `type253`/비번10자/storeId `976111`). **gitignore됨, 커밋금지**
- `baemin_reflow.py` — 배민 PDF 변환 프로토타입

---

## 3. 다음 할 일 (우선순위)

1. **[최우선] 미커밋 쿠팡 작업 커밋·푸시** — 동작하는데 git에 없음. `git add` 후 커밋.
2. **오라클 클라우드 이전** — `/tmp` 터널은 너무 불안정(URL 변동·초기화). 오라클 Always Free 리눅스에 `server.py` 올려 **고정 URL+상시가동**. 그 URL로 `CloudOrderClient.BASE` 교체 후 재빌드. (서버 코드는 `server-bridge/server.py`)
3. **쿠팡 신규주문 자동 폴링·자동출력** — 지금은 앱에서 수동 "출력". `status=PENDING` 주기 폴링 → 새 orderId만 1회 자동출력(중복방지).
4. **배민 완성**:
   - 서버에 `/baemin` 추가: PDF 업로드 → `pdftotext -layout` → 42칸 재배치(`baemin_reflow.py` 로직) → ESC/POS(init+EUC-KR+LF피드+ESC m절단).
   - 긴 옵션 줄 **줄바꿈** 처리(현재 truncate됨).
   - 앱: 변환 바이트를 내장 프린터로 출력(임의 ESC/POS 전송 경로).
   - **자동화**: 배민 클라가 새 주문을 **무프롬프트 PDF 자동저장**(Microsoft Print to PDF는 매번 경로 물음 → 자동저장 PDF프린터 필요) + 폴더 감시 → 서버 업로드.
5. **보안**: 쿠팡 비번이 캡처 HAR에 평문으로 있었음 → **비번 변경** 권장(변경 시 server-bridge/coupang_creds.json도 갱신).

---

## 4. 주의 / 비자명한 맥락

- **내장 프린터 호출법**: 유닉스 도메인 소켓 `/tmp/hkjt_buffer`(API≥30) | `/Flash/hkjt_buffer`. 프레임 `%04d(8+len)+"1001"+"0000"+payload`, payload=`'S'+포트+0x1C+데이터`. **포트 이름은 "INTERNAL"**(COM1/COM2는 서명패드). 등록 'A' 먼저. 구현: `KiccUdsClient.kt`.
- **프린터 폭 = 42칸**(제조사 테스트패턴 기준). 절단=**ESC m**(GS V 아님). 하단 피드는 **`ESC d` 무시됨 → 순수 LF(\n) 반복**으로! 점선은 `·`(중간점) 안 찍힘 → `.` 사용.
- **쿠팡 API**: `pos-api.coupang.com`, 쿠키인증(SESSION+unify-token+app-type/store-id/account-id/device-id). 서명헤더(pos-signature) **강제 안 됨**. 로그인 2단계: `/api/v2/auth/sign-in/user`(username/password/encrypt:false) → `/api/v2/auth/sign-in/store`(storeId). 세션 **~1시간 만료** → 서버가 403시 자동 재로그인. 주문: `/api/v2/stores/orders?status=PENDING|COMPLETED`, 상세 `/api/v1/stores/{id}/orders/{orderId}`.
- **paymentMethod**가 JSON null일 때 안드로이드 `optString`이 문자열 `"null"` 반환 → `isNull()` 체크 필수(이미 수정).
- **배민**: 주문/로그인 API 인증서고정으로 Fiddler에 안 잡힘(3회 캡처 모두 텔레메트리+광고CDN만). 주문은 **PubNub** 실시간 푸시. → API 따라하기 불가, **PDF 경유(B안)** 가 정답. 배민 PC클라는 Parallels VM에서 구동중.
- **HAR 위치**(`~/Downloads`): 쿠팡 `2026-06-14-141539.har`(로그인 포함, creds 출처) / `134746` / `140932`. 배민 `190650`/`191041`/`215500`(텔레메트리뿐). 배민 영수증 PDF 샘플 `dddd.pdf`.

---

## 5. 재개 방법

**레포**: `cd /Users/jiwonpark/Claude/Projects/android-printer` (branch `android-printer`)

**서버/터널 재기동**(/tmp 비워졌으면):
```bash
mkdir -p /tmp/apk-serve/uploads
cp server-bridge/server.py /tmp/apk-serve/server.py
cp server-bridge/coupang_creds.json /tmp/apk-serve/coupang_creds.json
cp app/build/outputs/apk/debug/app-debug.apk /tmp/apk-serve/print.apk   # 빌드 후
cd /tmp/apk-serve && nohup python3 server.py > /tmp/apk-server.log 2>&1 &
cloudflared tunnel --url http://localhost:8765 > /tmp/cf.log 2>&1 &      # 새 URL 확인
# → 새 URL을 CloudOrderClient.BASE에 반영하고 재빌드, spoo.me 단축링크 갱신
```

**빌드·배포**:
```bash
./gradlew :app:assembleDebug          # core 테스트: ./gradlew :core:test
cp app/build/outputs/apk/debug/app-debug.apk /tmp/apk-serve/print.apk
# 포스기에서 spoo.me/posapp2 재설치
```

**핵심 파일**:
- 내장프린터: `app/src/main/kotlin/kr/deliveryprint/printer/{KiccUdsClient,KiccInnerPrinter}.kt`
- 쿠팡 영수증: `core/src/main/kotlin/kr/deliveryprint/core/printer/CoupangReceiptFormatter.kt`
- 클라우드 주문: `app/src/main/kotlin/kr/deliveryprint/data/CloudOrderClient.kt`, `ui/MainActivity.kt`(CloudOrdersScreen)
- 서버: `server-bridge/server.py` / 배민변환: `server-bridge/baemin_reflow.py`

**첫 액션 추천**: ① 미커밋 쿠팡 작업 커밋 → ② 오라클 클라우드로 서버 이전(고정 URL) → ③ 배민 PDF 변환 서버/앱 통합.
